Compiling and running the app:

make depend Build the dependencies of the application.
make main Build the applcation executable.
make run Run the application.

A file called "DATABASE.txt" is created once an entry has been saved.